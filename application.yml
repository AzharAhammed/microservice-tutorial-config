spring:
  application:
    name: Default-Application
    eureka:
      client:
        fetch-registry: true
        register-with-eureka: true
        service-URL:
          defaultzone: http://localhost:8761/eureka/
